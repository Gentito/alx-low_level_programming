#include "main.h"
/**
 * _abs - compute an absolute value of integer
 * @n: interger value
 * Return: absolute value
 */
int _abs(int n)
{
if (n > 0)
{
return (n);
}
else if (n < 0)
{
return (-n);
}
return (0);
}
